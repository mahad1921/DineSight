# DineSight – copy to .env and fill in values. Do not commit .env.
# Local dev: optional; app falls back to SQLite and development defaults.

# Database URL. Use PostgreSQL in production (e.g. Railway Postgres plugin).
# Railway may give postgres://; the app converts it to postgresql:// automatically.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# DATABASE_URL=sqlite:///./dinesight.db

# Set to "production" to enable secure cookies (HTTPS-only) and production behavior.
# ENVIRONMENT=production

# Optional: set a cookie domain (e.g. .railway.app). Leave unset to use current host.
# COOKIE_DOMAIN=

# Optional: secret for future use (e.g. signing). Change in production.
# SECRET_KEY=your-secret-key
